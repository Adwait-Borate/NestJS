export interface UserDataDTO{    //interface or DTO (data transfer object)
    name:string;
    email:string;
    age:number;
  }

//it is simply used to define the datatype, similar to models in database.